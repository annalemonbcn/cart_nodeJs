const addressesCollection = "addresses";
const addressModel = "Address";

const usersCollection = "users";
const userModel = "User";

const cartsCollection = "carts";
const cartModel = "Cart";

const productsCollection = "products";
const productModel = "Product";

const collectionNames = {
  addressesCollection,
  usersCollection,
  cartsCollection,
  productsCollection,
};

const modelNames = {
  addressModel,
  userModel,
  cartModel,
  productModel,
};

const COLORS = [
  "purple",
  "black",
  "red",
  "orange",
  "navy",
  "white",
  "broom",
  "green",
  "yellow",
  "grey",
  "pink",
  "blue",
];

export { collectionNames, modelNames, COLORS };
