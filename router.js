const baseRoute = "/api";

const productsRoute = baseRoute.concat("/products");
const cartsRoute = baseRoute.concat("/carts");

const routes = {
  productsRoute,
  cartsRoute,
};

export default routes;
